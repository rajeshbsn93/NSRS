/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Enable_disableFormService } from './enable_disableForm.service';

describe('Service: Enable_disableForm', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Enable_disableFormService]
    });
  });

  it('should ...', inject([Enable_disableFormService], (service: Enable_disableFormService) => {
    expect(service).toBeTruthy();
  }));
});
