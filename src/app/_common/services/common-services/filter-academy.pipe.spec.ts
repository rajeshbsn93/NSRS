import { FilterAcademyPipe } from './filter-academy.pipe';

describe('FilterAcademyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAcademyPipe();
    expect(pipe).toBeTruthy();
  });
});
